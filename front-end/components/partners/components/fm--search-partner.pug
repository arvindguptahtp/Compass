extends /forms/fm--search

append model
    -
        form
            .instance({
                class: 'fm--search-partner',
                element: {
                    class: "thm--partners"
                }
            })
            .data({
                fieldsets: partners.fm_search
            })


prepend view
