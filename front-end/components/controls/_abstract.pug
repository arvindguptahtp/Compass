extends /_vefa

block init
    -
        vefa = new Vefa({
            class: "ctrl",
            data: {
                url: false,
                target: false,
                label: false
            },
            element: {
                "o-base": "row--pMiddle--sMiddle--xAuto rhy--xStart25 ico--sm",
                "o-condensed": "rhy--xStart50",
            },
        })


//- block instance
//-     yield


block view
    a(
        class=ctrl.class
        href=v(ctrl.url)
        target=ctrl.target
    )&attributes(ctrl.element)

        //- block icon

        block bd
            span= ctrl.label
