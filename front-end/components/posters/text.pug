extends _abstract

block subclass
    -
        vefa
            .define({
                class: "poster--text",
                data: {
                    title: wrap_str("Generic")
                }
            })
            .mixin({
                element: {
                    "o-base": "row--pStart--sMiddle"
                }
            })
